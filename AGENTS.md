# AGENTS.md — Movilvistas (SennAccess)

Aplicación Android (Kotlin + Jetpack Compose) en `/home/andresvolveras/Escritorio/Movilvistas-main`.
No es repo git (el `.git` original se perdió). El código está subido a `git@github.com:Andres777233/VistasAndroidDef1.git` (rama `main`, commit "VersionBackend"). Compilar: `./gradlew assembleDebug` (APK en `app/build/outputs/apk/debug/app-debug.apk`).

## Decisiones
- 2026-08-12 — Todos los GETs contra la API: Aprendiz Novedades (`AprendizDashboardViewModel.cargarNovedades` → `GET /novedades`); Admin AccesoAprendices/AccesoInstructores conectados al historial real (`GET /admin/ingresos`+`/admin/users`); ActualizarUsuario prellena desde el `UsuarioApi` del GET `/admin/users`; CrearUsuario carga roles desde `GET /admin/roles`; ReporteInstructor carga instructores desde `GET /admin/users` filtrado por rol. Se eliminaron los datos estáticos `RegistroAccesoMock`. Backend: `GET /novedades` movido a `auth:sanctum` para que el aprendiz pueda consultarlo.
- 2026-08-12 — El proyecto se subió a GitHub como `VistasAndroidDef1` (repo ya existente con la versión previa). Estrategia: se clonó el repo, se superpuso el código local sobre el clon y se hizo push como "VersionBackend" (un solo commit con todo el código). El `.gitignore` ahora excluye `.kotlin/` y `*.keystore`. El local sigue sin repo git; para próximos push clonar `git@github.com:Andres777233/VistasAndroidDef1.git`, superponer, commitear y pushear. (reemplazada)
- 2026-08-12 — Cierre de sesión con registro de salida: los 3 dashboards (aprendiz/instructor/admin) ahora llaman a `POST /api/logout` de forma best-effort (`AuthRepository.logout`) antes de limpiar la sesión; el backend crea el `Ingreso` tipo `Salida`. `MainActivity.kt` define `cerrarSesion: () -> Unit` con `rememberCoroutineScope`.
- 2026-08-12 — Historial de accesos con estado: `RegistroAccesoAdmin` incluye `tipo` (Entrada/Salida); la tarjeta de `HistorialAdmin.kt` muestra etiqueta ENTRADA (verde) / SALIDA (naranja) con su hora. `HistorialIngresosView` del instructor agrega columna ESTADO (`● INGRESO`/`● SALIDA`). El aprendiz ya veía su historial con estado.
- 2026-08-12 — Registrar equipo con accesorios (instructor): nueva `RegistrarEquipoView` (`Aprendiz/RegistrarEquipo.kt`) con formulario de vidrio en un scroll que revela secciones (datos portátil → lleva accesorios Sí/No → cantidad 1/2/3 → detalle por accesorio: Mouse/Audífonos = marca+color+inalámbrico, Teclado = marca+color). Guarda vía `POST /api/admin/equipment` (`EquipoRepository.registrar`). Serial = últimos 5 dígitos numéricos.
- 2026-08-12 — Backend equipos: `POST /api/admin/equipment` ahora es accesible a admin e instructor (se movió fuera del grupo `admin`; index/delete siguen solo admin); `EquipmentController::store` asigna `fk_id_usuario` desde el token (ya no se envía en el body) y guarda `equipo_accesorios` (JSON array de {tipo,marca,color,inalambrico}). Migración `2026_08_12_000001_add_accesorios_to_ingreso_equipos_table.php`.
- 2026-08-11 — Conectar pantallas a la API real (Retrofit) con patrón `CargaUiState` (Loading/Success/Error) y fallback a mocks de `MockData.kt` cuando no hay sesión (`SessionManager.token == null`) o la llamada falla. Los mocks viven SOLO en `data/mock/MockData.kt`, nunca en pantallas.
- 2026-08-11 — `InstructorDashboard.kt` y `AprendizDashboard.kt` quedan en el mismo paquete `com.example.sennaccess.Aprendiz`, comparten `StatCard` y `TableContainer`. Los ViewModels se agrupan en `com.example.sennaccess.ui`.
- 2026-08-11 — Conexión a la API con doble ruta (solo celular físico, sin emulador): `RetrofitClient.conServicio{}` intenta USB (`127.0.0.1:8000`, con `adb reverse tcp:8000 tcp:8000`) y si hay `IOException` reintenta por WiFi (`192.168.1.19:8000`, `php artisan serve --host 0.0.0.0`). Connect timeout 3s para fallback rápido. La IP WiFi es DHCP: si cambia, actualizar `BASE_URL_WIFI`.
- 2026-08-11 — Android Studio Flatpak (`com.google.AndroidStudio`): el error `DirectoryLock cannotActivate` (BindException "dirección ya se está usando") ocurre al abrir una 2ª instancia mientras otra está arrancando (la 1ª tarda ~1 min). Fix: matar el árbol de procesos `studio`/`android-studio-wrapper`/`bwrap`, `./gradlew --stop`, y borrar el candado viejo en `~/.var/app/com.google.AndroidStudio/config/Google/AndroidStudio<version>/.lock`. Abrir UNA sola instancia y esperar.
- 2026-08-11 — Credenciales de prueba backend (MySQL `senaaccess`, en `SenaAccess-Repo2-master`): `admin@sena.edu.co`, `instructor@sena.edu.co`, `aprendiz@sena.edu.co` con contraseña `12345678` (reseteada por tinker). La app redirige por rol automáticamente. Servidor: `php artisan serve --host 0.0.0.0 --port 8000` (necesario `--host 0.0.0.0` para WiFi).
- 2026-08-12 — Documentación de código: los comentarios NUEVOS van solo con `//` (línea propia, texto plano, sin asteriscos/emojis/cajas); los KDoc existentes de `ui/ios/` se conservan intactos y no se duplican. Cada archivo abre con 2-4 líneas `//` de cabecera. No tocar nunca líneas de código al documentar.
- 2026-08-11 — Navbar y menú en estilo vidriado verde: `topBarBackground` en `ui/theme/Color.kt` pasa de negro a `Color(0xFF0E2013)` (oscuro) / `Color(0xFFE9F5EC)` (claro); `IosGlassTopBar` e `IosCollapsibleHeader` añaden brillo `SenaGreen.copy(0.10f)` al inicio de su degradado (aplica también a sub-pantallas). El menú de las 3 rayas usa el nuevo `IosGlassDropdownMenu` de `ui/ios/GlassComponents.kt` (containerColor glass, esquinas 20dp, borde sutil) en AdminTopBar/AprendizTopBar/InstructorTopBar.
- 2026-08-11 — RegisterScreen: botón "CREAR CUENTA" → "REGISTRARSE"; al pulsarlo muestra un AlertDialog glass "Solicitud enviada" con botón "Okey" que regresa al login. Solo vista (no envía datos).
- 2026-08-11 — Conexión teléfono↔backend: flujo diario = conectar el celular por USB → ejecutar `bash ~/Escritorio/SenaAccess-Repo2-master/iniciar-servidor.sh`. El script levanta `php artisan serve --host 0.0.0.0 --port 8000` (si no corre), aplica `adb reverse tcp:8000 tcp:8000` y deja un watcher en segundo plano que reaplica el reverse cada 3s mientras haya teléfono conectado (log: /tmp/sennaccess-watcher.log). Así el reverse nunca queda vacío tras desconectar/reconectar el cable. Síntoma de fallo anterior: servidor solo en 127.0.0.1 o reverse vacío → login "No se pudo conectar al servidor" sin navegar. Nota: daemon de Gradle del host procesa el sync de Android Studio Flatpak → error "The specified initialization script '/tmp/ijMapper1.gradle' does not exist" (AS escribe init-scripts en su /tmp privado del sandbox, el daemon host no lo ve). Fix: `./gradlew --stop` antes de sincronizar en AS.


